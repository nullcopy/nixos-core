# Placeholder — replace with the real file generated during install:
#   nixos-generate-config --root /mnt --show-hardware-config > hardware-configuration.nix
# (scripts/nixos-install.sh in nixos-core does this automatically.)
{ ... }: { }
